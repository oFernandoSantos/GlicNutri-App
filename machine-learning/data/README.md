# Dados ML — paciente-dia

| Ficheiro | Descrição |
|----------|-----------|
| `sample_glicnutri_patient_day.csv` | Exemplo versionado para desenvolvimento sem Supabase. |
| `glicnutri_patient_day_export.csv` | Gerado localmente por [`../scripts/export_supabase_csv.py`](../scripts/export_supabase_csv.py); **ignorado pelo Git** (ver [`.gitignore`](./.gitignore)). |
| `export_manifest.json` | Opcional: metadados + SHA-256 (`--manifest`) para evidência sem commit do CSV completo. |

Ver procedimento em [`../EXPORT_CSV.md`](../EXPORT_CSV.md).
