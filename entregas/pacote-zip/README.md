# Pasta de saída dos ZIP de entrega

Os ficheiros `GlicNutri-repositorio-*.zip` gerados por [`scripts/build-zip-entrega.ps1`](../../scripts/build-zip-entrega.ps1) são colocados aqui por convenção.

Estes ZIP são **gerados localmente** e, por omissão, **não devem ser commitados** (ver `.gitignore` na raiz).
