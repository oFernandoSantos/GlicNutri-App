/**
 * Portal profissional usa a mesma paleta do paciente (verde #4FDFA3, fundos claros, cards com borda suave).
 * Mantemos este arquivo para imports estáveis; tokens são idênticos a temaVisualPaciente.
 */
import { patientTheme, patientShadow } from './temaVisualPaciente';

export const nutriTheme = patientTheme;
export const nutriShadow = patientShadow;
